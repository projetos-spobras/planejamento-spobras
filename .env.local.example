# Supabase (public - safe to commit)
VITE_SUPABASE_URL=https://kuxryjfjbsmbhcjwgsgt.supabase.co
VITE_SUPABASE_ANON_KEY=

# Mapbox (fetched from Supabase Edge Function in production - only needed locally as fallback)
# VITE_MAPBOX_TOKEN=pk.....

# SQL API credentials (ONLY for local development via Vite proxy)
# In production, these are stored as Supabase secrets and never exposed to client
VITE_API_SQL_USERNAME=ApiEmpreendimentos
VITE_API_SQL_PASSWORD=
